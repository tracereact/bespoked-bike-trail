{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "preinstall": "npm i nodemon -g; npx prisma generate",
    "start": "nodemon server.js",
    "deploy": "npx prisma migrate dev --name init; nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "prisma": {
    "seed": "node prisma/seed.js",
    "schema": "prisma/schema.prisma"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "prisma": "^4.4.0"
  },
  "dependencies": {
    "@fastify/cookie": "^8.3.0",
    "@fastify/cors": "^8.1.0",
    "@fastify/sensible": "^5.1.1",
    "@prisma/client": "^4.4.0",
    "dotenv": "^16.0.3",
    "fastify": "^4.8.1"
  }
}
